# Conventions

- Keep Word source files out of Git via `.gitignore` rules for `*.doc` and `*.docx`.
- Preserve user-requested filenames exactly, including misspellings such as `Referece.md`.
- For assignment references, extract only the chosen task content; avoid unrelated task sections.
- Documentation should avoid historical wording that depends on prior project context.