# Core

- Repository currently contains assignment/reference material, not an application implementation.
- `Referece.md` is the extracted Task 2 Markdown reference from the ignored Word source.
- Original `.doc`/`.docx` files are intentionally ignored by `.gitignore`.
- Superpowers planning/spec folders exist at `docs/superpowers/plans/` and `docs/superpowers/specs/` with `.gitkeep` placeholders.

Read `mem:tech_stack` for current tooling state, `mem:conventions` for repo-level naming/content constraints, `mem:suggested_commands` for useful commands, and `mem:task_completion` before finishing code changes.