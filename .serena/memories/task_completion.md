# Task Completion

- For document-only changes: inspect the final file directly and verify requested scope/content.
- For Git ignore changes: run `git status --short --ignored` and, when relevant, `git check-ignore -v <path>`.
- No test/lint/build commands exist yet because there is no application code or package manifest.